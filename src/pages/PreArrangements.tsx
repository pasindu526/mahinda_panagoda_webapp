import React from "react";
import Navbar from "../components/common/Navbar";

const PreArrangements: React.FC = () => {
  return (
    <div>
      <section className="w-full">
        <Navbar />
      </section>
    </div>
  );
};

export default PreArrangements;
